{% raw -%}
{% import "ipa_macros.tmpl" as ipa -%}
{%- endraw %}
#!/bin/bash

CSR="$(mktemp)"
certutil -R -a -o "$CSR" {{ options|join(' ') }} "$@"
echo ipa cert-request "$CSR" --profile-id '<profile>' --principal '<principal>'
